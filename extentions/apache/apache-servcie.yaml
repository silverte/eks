apiVersion: v1
kind: Service
metadata:
  name: apache-service
  namespace: esp-fo-prd
  labels:
    app: apache-server
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: apache-server
